# Agent-toolkit Speculoos review gate

Make shared agent-toolkit pull requests use a fail-closed, exact-head Mergeguez review lane without reactivating legacy Pullfrog or CodeRabbit automation.

The repo-local metadata records public workflow policy only. Credentials, host paths, prompts, raw model output, and private runtime details remain outside the repository.
